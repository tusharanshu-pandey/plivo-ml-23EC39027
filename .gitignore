# --- Provided input data: large audio, not needed in the repo ---
# (the grader runs predict.py on THEIR own folder; we keep labels.csv, drop wavs)
*.wav

# --- Provided archives ---
*.zip

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Local scratch / experiment outputs (deliverable predictions are committed explicitly) ---
scratch/
*.log
base.csv
mine.csv

# --- OS / editor junk ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
